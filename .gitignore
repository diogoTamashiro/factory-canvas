# === Secrets / env (NUNCA subir pro git) ===
.env
.env.*
!.env.example

# === Rust ===
/target
**/*.rs.bk
Cargo.lock  # descomente se for binário (não lib): mantém reprodutível

# === Python (solver) ===
.venv/
venv/
__pycache__/
*.pyc
*.egg-info/

# === Local data (capturas, sqlite) ===
data/
*.db
*.sqlite
*.sqlite3

# === IDE / SO ===
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# === Continue / Ollama (não versionar) ===
.continueignore
sessions/
dev_data/
